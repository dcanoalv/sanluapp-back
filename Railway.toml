# Configuración del proyecto Railway para una app Spring Boot

[build]
  # Especifica que se trata de una aplicación Java
  builder = "maven" # o "gradle", dependiendo de tu proyecto

[deploy]
  # Comando para ejecutar la aplicación
  start = "java -jar target/sanluapp-0.0.1-SNAPSHOT.jar"
